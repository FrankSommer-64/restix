#!/bin/bash

case "$1" in
  remove)
    rm -f /usr/local/bin/restix
    rm -rf /opt/restix
    ;;
  purge)
    rm -f /usr/local/bin/restix
    rm -rf /opt/restix
    rm -rf $HOME/.config/restix
    ;;
esac
